export enum RouteKey {
  // Main pages
  HOME = "Home",
  LOGIN = "Login",
  PROFILE = "Profile",
  FOLDER = "Folder",
  NEW_PROFILE = "NewProfile",
  NEW_TAX = "NewTax",
}

export enum SearchParams {
  Query = "q",
  View = "view",
}
